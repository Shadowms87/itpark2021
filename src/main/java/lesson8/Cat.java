package lesson8;

/*public class Cat {

    String name;
    int age;
    String poroda;
    String bossName;

    public boolean equals (Object cat) {
        if (cat instanceof Cat c) {
            return Object.equals(name, c.name)
                    && age == c.age
                    && Object.equals(poroda,c.poroda);
        }
        return name == cat.name && age == cat.age
                && poroda == cat.poroda;


    }
        public boolean equals (Cat cat) {
        return super.equals(cat);
        }

}*/
